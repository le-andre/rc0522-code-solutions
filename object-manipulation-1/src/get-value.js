/* exported getValue */
/* the function getValue will be called with two parameters, object and key.
the return avalue wil be the object found at the key property */
function getValue(object, key) {
  return object[key];
}

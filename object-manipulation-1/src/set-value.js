/* exported setValue */
/* When the setValue function is called with three parameters, object, key and value,
A key and value will be added to the object. */
function setValue(object, key, value) {
  object[key] = value;
}

/* When the setValue function is called with three parameters, object, key and value,
the object at key will be assigned value. */
